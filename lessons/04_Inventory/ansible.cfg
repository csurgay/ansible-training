[defaults]
inventory = ./host_inventory
remote_user = local
ask_pass = false
interpreter_python = /usr/bin/python3

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false

